# 连续指标第一阶段对照报告

日期：2026-09-13。数据：phaseone_data 的八个区域、完整14天。
本轮仅检测 traffic_flow_metrics 和 node_metrics，未接入离散数据。完成本地对照后，经用户批准提交B和C traffic。

## 实验条件

- 基线：触发前滚动取过去20min，触发后冻结；无异常超过源内 EVENT_GAP 后解冻。
- traffic=5sigma，node=6sigma，源内 EVENT_GAP=5min，全局 EVENT_GAP=3min。
- 单源和融合输出上限均为500。没有凑足292个窗口。
- 保留原有全局打分、拓扑角色奖励、门槛及时间冲突筛选。
- 保留原有边界：首个有效分钟至最后有效分钟+1min，没有额外扩边。
- 保留原有长段过滤：最后异常点减首个异常点>30min时删除；加一个采样分钟后，源内段可能达到31min。本轮不改这一规则。
- 旧全局冲突逻辑比较的是开始时间相距20min，不是上个故障结束后间隔20min。仅作为控制条件，不视为官方规则的严格实现。

## 版本定义

| 版本 | 相对前一版的改动 |
|---|---|
| A original | 复现原算法，累计计数器直接用原值，零方差不报警 |
| B counter rate | 所选累计计数器转为每秒增量；首样本不生成速率，重置和非正时间间隔跳过；缺失不补零 |
| C zero variance | B基础上，为零方差基线设置最小有效变化幅度；非零方差的判定与magnitude公式保持原样 |
| D series identity | C基础上，按traffic完整对象身份区分序列，去除重复或冲突样本；不跨超过5min的计数器采样间隔生成速率 |

D包含身份、去重和长采样间隔防护，是数据整理组合对照。如果线上有差异，不能直接归因于其中某一项，需要再拆分。
C触发新的零方差事件后会改变冻结状态和后续基线，因此整体段分布可能改变，即使非零方差公式没有改变。

零方差最小变化幅度是实验参数，不是已经验证的最佳值：累计量速率>1/60每秒，比例或loss_rate变化>0.01，latency/jitter变化>0.005秒，CPU/IO利用率变化>1，load变化>0.5，process_count变化>5。未配置的字段保持旧判定。
这些门槛只处理零方差，尚未解决极小但非零方差或单侧异常方向问题。

## 输入审计

| 数据源 | 行数 | 旧序列键下重复时间戳 |
|---|---:|---:|
| traffic | 421,227 | 0 |
| node | 1,450,970 | 0 |

traffic累计量没有观察到负增量重置；D跳过5,315个超过5min的计数器采样间隔。这个数字按指标计数，不是故障数。
目前没有证据说明本数据集的重复时间戳是低分主因。完整身份和去重仍是正确的数据处理防护。
第一次全量运行退出，没有Python异常输出，当时进程内存约4.6GB。移除冗余node序列副本并复用完全相同的node检测结果后，全量重跑成功。

## traffic对照

| 版本 | 保留小段 | 单点段 | 删除长段 | 最终窗口 | 平均窗口长度/min |
|---|---:|---:|---:|---:|---:|
| A | 4,841 | 3,282 | 2,488 | 304 | 8.332 |
| B | 5,498 | 3,552 | 556 | 299 | 6.722 |
| C | 6,104 | 3,803 | 643 | 316 | 7.354 |
| D | 6,091 | 3,800 | 643 | 316 | 7.297 |

B的长段删除数比A减少约77.7%，DNS/Web错误指标开始产生原版没有保留的小段，支持“累计量直接检测会制造拖长异常”的判断。它不等于已经证明召回或AD提升。
C的dns_flow_error_total小段从B的190增至663，提示零方差规则补充了错误信号，也可能带入零星正常错误，需线上验证。
C和D虽然均为316个窗口，起止区间并不完全相同，不能用相同数量认定两个版本等价。

## node及融合对照

| 版本 | node小段 | node单点段 | node删除长段 | node单源窗口 | 融合窗口 | 融合平均长度/min |
|---|---:|---:|---:|---:|---:|---:|
| A | 67,000 | 41,488 | 1,107 | 500 | 500 | 26.998 |
| B | 67,000 | 41,488 | 1,107 | 500 | 500 | 26.960 |
| C | 67,054 | 41,526 | 1,106 | 500 | 500 | 27.060 |
| D | 67,054 | 41,526 | 1,106 | 500 | 500 | 27.060 |

A/B的node处理完全相同，因为所选node字段没有累计计数器。C/D的node也相同，因为本数据没有node重复时间戳或traffic身份变化。
以上500均是输出达到上限，不能理解成全部候选恰好500个。
D融合版414/500个窗口长度>=25min，498/500标记含node+traffic。这不代表强共振：两个源只要在整个长窗口内出现就会进入sources，且源内小段间的空隙也被填为有效分钟。
当前全局聚合在高密度node小段下容易连续激活，再被29min切段条件切成长窗。因此，数值检测和全局聚合存在共同问题，不能只归罪于sigma或冻结基线。

### node小段最多的指标（D，6sigma）

| 指标 | 小段数 |
|---|---:|
| load1 | 14,455 |
| cpu_usage | 12,220 |
| disk_io_util | 12,093 |
| load5 | 10,121 |
| disk_write_rate | 6,214 |
| process_count | 6,181 |
| memory_available_ratio | 3,061 |
| open_fd_ratio | 1,728 |
| filesystem_used_ratio | 630 |
| disk_read_rate | 226 |
| inode_used_ratio | 125 |

前四项占约72.9%，单点段占约61.9%。这验证了node候选高密度和碎片化，但没有标签时不能把67,054个小段都判为误报。
load1/load5等相关指标不应简单当作独立证据重复加分。CPU、内存、磁盘空间等也应考虑异常方向、绝对压力和持续性。

### 独立敏感性对照（均以D为基础）

| 改动 | node小段 | 相对6sigma减少 | 融合窗口 | 平均长度/min | >=25min窗口 |
|---|---:|---:|---:|---:|---:|
| node 6sigma | 67,054 | - | 500 | 27.060 | 414 |
| 仅node改7sigma | 52,261 | 22.1% | 500 | 22.524 | 259 |
| 仅node改8sigma | 42,324 | 36.9% | 500 | 19.218 | 191 |
| 仅node权重1降至0.3 | 67,054 | 0% | 500 | 26.796 | 410 |

降权只改变小段magnitude贡献，不改变固定source奖励、开窗门槛、源内小段或分钟激活；不能期待它单独消除连续激活。
升sigma有效减少段数和长窗，但仍不代表误报率降低，可能同时漏掉资源故障。暂不优先提交全量node融合版本。

## 历史回执复用

以下回执本轮重新查询确认，没有消耗新提交机会。

| 历史版本 | 提交ID | AD | 本轮用途 |
|---|---|---:|---|
| traffic-only 5sigma，global gap3 | 1789156640101 | 3.8561178953376585 | A traffic的304个区间全部逐一复现一致，可直接复用AD对照 |
| frozen20 gap5 balanced | 1789134745058 | 1.1163945073603347 | 数据和聚合条件不同，仅供历史参考 |
| dynamic 5sigma | 1789129491980 | 8.36555458835389 | 基线、候选和聚合同时改变，仅供历史参考 |

没有已验证的本轮A融合版线上回执，不给它套用其他版本的AD。历史AD复用仅针对时间窗口，不保证辅助RCA/类别填充完全相同。

## 提交建议

1. 最先提交B traffic：唯一核心变化为累计量差分，直接和已确认的A traffic AD=3.8561比较。
2. 随后提交C traffic：与B比较，判断零方差规则的收益与误报代价。
3. 再决定是否提交D traffic：与C比较数据身份和长间隔防护；目前变化较小，优先级低于B/C。
4. node先按资源指标族设计“同节点互补指标+异常方向+持续性”门槛，不先花机会提交四个都饱和的融合版。强单节点资源故障允许独立候选，不强制跨多设备共振。
5. 输入版本有回执后，再固定输入做三种基线对照；随后分开测试边界、sigma及两级gap。

本轮11份提交文件已通过既有格式脚本和validator校验；B和C traffic经批准已提交，其他新版本尚未提交。
补全脚本沿用原有RCA/类别占位逻辑；其类别输出不代表本轮识别了故障类型，不能用RCA或类别分数判断AD效果。

## 产物与验证

- tools/compare_continuous_stage1.py：可复现全量实验，默认不生成提交，--prepare-submissions仅补全和校验，仍不会上传。
- tools/test_continuous_stage1.py：5项测试通过，覆盖计数器重置/缺失间隔、零方差有效幅度、长段记录、普通方差兼容、不同traffic对象分离。
- outputs/continuous_stage1/report.json：完整输入审计、逐指标小段统计、参数及历史复现检查。
- outputs/continuous_stage1/*_windows.jsonl：各源及融合候选。
- outputs/continuous_stage1/*_submit.jsonl：11份可提交文件。

未使用隐含标签评估。B traffic的线上回执支持累计量修正，但尚不能证明冻结20min优于滚动69min。

## B traffic线上回执

2026-09-13，用户批准后提交B_counter_rate_traffic_submit.jsonl，299个预测窗口。

| 版本 | 提交ID | AD | 总分 |
|---|---|---:|---:|
| A traffic历史对照 | 1789156640101 | 3.8561178953376585 | 5.527350772049987 |
| B traffic累计量转速率 | 1789285247574 | 8.427822230876133 | 11.010014011698052 |
| C traffic零方差判定 | 1789285354387 | 9.535391740361836 | 13.439501329402935 |

相对A，B的AD增加4.571704335538474分，约提高118.6%。相对B，C再增加1.107569509485703分，约提高13.1%；相对A累计增加5.679273845024177分。C提交后服务器返回该团队当天剩余3次机会。
基线、sigma、两级gap及窗口边界逻辑保持一致。B/C回执支持累计量转速率和零方差判定均有实际收益。没有逐故障匹配明细，尚不能区分收益来自召回、边界或查准率各占多少。
不优先提交D：它在C基础上同时加入对象身份、去重和长采样间隔防护，无法把回执差异归因于其中一项。下一项优先设计node资源指标族筛选，再决定是否以C traffic为锚点接入node。

## 基线对照：完整traffic数据，本地结果

保持C输入处理、5sigma、源内gap=5min、全局gap=3min、长段过滤和全局聚合不变，仅替换基线。C的316个起止区间全部逐一复现一致。

| 基线 | 小段 | 窗口 | 平均窗口/min | 中位数/min | <=3min窗口 | 与C起止完全相同 |
|---|---:|---:|---:|---:|---:|---:|
| C 冻结20min | 6,104 | 316 | 7.354 | 3 | 166 | 316 |
| 滚动69min | 5,626 | 243 | 3.667 | 2 | 152 | 77 |
| 14天等分292块 | 3,139 | 166 | 1.578 | 1 | 154 | 68 |

滚动69min每点取[t-69min,t)历史，包含先前异常，不冻结，不加入当前点，不用未来数据；不足12个历史样本不检测。
整块从数据最早时间起等分14天，每块约69.0411min；每个(node,metric)独立用所在块的全部样本计算均值和总体方差，包含当前和未来样本。区间为左闭右开，跨块候选仍按gap合并，不要求每块恰好一个事件。9,310个指标块不足12个样本被跳过，按指标计数，不是9,310个故障。

滚动版152/243、整块版154/166个窗口<=3min。窗口短是可观察事实，异常被基线吸收只是待验证解释，不能用窗口数推定准确率。
两个新版本均完成JSONL补全、格式校验和起止区间保持检查；滚动69min经用户批准已提交，整块版尚未提交。整块版本使用未来数据，仅作为离线对照；如需提交，需符合赛题数据使用规则。
滚动69min回执已低于C；当前保留冻结20min，不据此推断所有滚动基线都较差。gap仍保持不变，后续分别测试源内gap与全局gap。

只保存一份精简差异记录：outputs/traffic_baseline_compare/summary.json，含逐指标统计和提交文件SHA256，不另生成长报告。
新提交文件：outputs/traffic_baseline_compare/C_rolling69_submit.jsonl、C_block292_submit.jsonl。
新增5项基线测试通过，覆盖历史因果性、旧样本过期、整块异常稀释、跨块合段和无效块区间；原有5项处理测试也通过。

### 滚动69min线上回执

2026-09-13，经用户批准提交一次，团队2097279104209394157；提交后服务器返回当天剩余2次。

| 基线 | 提交ID | 窗口 | AD | 总分 |
|---|---|---:|---:|---:|
| C 冻结20min | 1789285354387 | 316 | 9.535391740361836 | 13.439501329402935 |
| C 滚动69min | 1789287511713 | 243 | 6.914324370032135 | 9.626653137155424 |

滚动版AD下降2.621067370329701分，约27.5%。仅改变基线，当前参数下冻结20min优于持续更新的滚动69min。缺少逐故障TP/FP/FN和Dice明细，不能判断下降中漏检、时间边界与查准率各自贡献，也不能证明基线污染是唯一原因。
推荐后续以C冻结20min为对照，先单独延长全局gap（3至8或10min）；源内gap仍为5min。292块版本保留待选择，不自动提交。

### 全局gap10min线上回执

2026-09-13，经用户批准提交C_frozen20_gap10_submit.jsonl；团队2097279104209394157，提交ID1789287832287。服务器返回当天剩余1次。本次未提交292块版。

| 全局gap | 小段 | 窗口 | 平均窗口/min | AD |
|---|---:|---:|---:|---:|
| 3min | 6,104 | 316 | 7.354 | 9.535391740361836 |
| 10min | 6,104 | 315 | 9.702 | 9.509143835616447 |

gap10总分13.152979452054804，AD下降0.026247904745389分。没有净提升，当前保留C的全局gap3、源内gap5。此结果只说明本次改动净效果接近持平，不证明合窗没有价值，也不能分解误合并与碎片修复各自作用。
运行前用相同输入和小段再次复现C的316个gap3区间；gap10输出和补全提交区间保持一致，validator通过。精简摘要在outputs/traffic_gap10_compare/summary.json。

### 292块为什么偏短、偏少

整块均值方差包含故障样本，可能稀释持续故障；但9310个指标块不足12个样本、全局开窗门槛和时间冲突筛选也影响最终窗口数。不能仅凭166个窗口断定某一个原因。
简化为正常和故障两个固定平台，故障样本占比p，则故障点相对整块均值的标准分数为sqrt((1-p)/p)，与偏移幅度无关。69min块内均匀采样、故障5min时约3.58，故障10min时约2.43；降到4sigma也不能保证捕获这种持续偏移。
4.5/4sigma可能补充短弱事件，同时增加噪声，值得作为独立阈值对照，不能作为整块基线污染的确定修复。样本不足导致的跳过不会被降低sigma解决。本次没有修改或提交整块版阈值。

### 292块版sigma本地对照

2026-09-13，用相同完整traffic数据比较5sigma、4.5sigma、4sigma，累计量转速率及零方差最小幅度保持C设置。源内gap5、全局gap3，块内整体均值及总体方差、12样本最低要求和全局规则都不变；本次没有比赛提交，也未生成新的补全提交文件。
5sigma的166个区间与上一轮整块版逐一复现一致。全局新增统计只增加观测字段，不改变筛选结果。

| sigma | 小段 | 单点段占比 | 筛选前候选 | 最终窗口 | 平均窗口/min | 中位数/min | <=3min窗口 | >=5min窗口 |
|---|---:|---:|---:|---:|---:|---:|---:|---:|
| 5 | 3,139 | 94.84% | 185 | 166 | 1.578 | 1 | 154 | 5 |
| 4.5 | 4,042 | 92.40% | 212 | 183 | 1.891 | 1 | 159 | 15 |
| 4 | 5,434 | 90.30% | 245 | 208 | 2.322 | 1 | 170 | 28 |

5至4sigma，小段增加约73.1%，窗口增加约25.3%；4sigma仍有170/208（81.7%）窗口<=3min，仅8个窗口>=10min，最长14min。三版均未触及500上限；样本不足跳过的指标块三版同为9310，降低sigma无法恢复这些块。
4sigma相比5sigma保留133个完全相同的区间，新增或改变75个区间，33个原区间不再完全一致。不能把这75个都视为新增正确故障，也不能把那33个都视为漏检，因为阈值变化会改变合窗和重排。

**可行性判断：块内整体均值方差不优先作为主AD基线，值得保留为尖峰补充对照。**
实际降阈值增加了段数及持续窗口，不能说没有作用；但单点段仍超过90%，与C冻结20min约62.3%的单点段比例、4.178min的源内平均段长相比，整块4sigma源内平均段长仅1.229min。结合整块统计稀释平台偏移的数学性质，它更像检测相对块内分布的尖峰，尚未表现出稳定追踪持续偏移的能力。
AD需要有效区间匹配，不能只优化告警数量。举例：真实10min、预测2min且完全包含在真实区间内，Dice仍仅0.333。这里是评分风险说明，不是对未标注候选的准确率评估。
没有真值或新回执，不能宣称4sigma线上一定优于5sigma，也不能判定这些短窗口全是误报。未对整块版本作线上分数承诺。

后续优先保留已获得AD9.5354的C冻结20min。若继续研究分块思路，建议保留大块作为搜索范围，在块内找稳定历史子窗口或用稳健基线降低异常污染，再单独对照；不按块强制一个事件，不靠降到3sigma或凑足292个窗口解决。
本轮保存三个*_windows.jsonl及一份outputs/block_sigma_compare/summary.json，包含逐指标统计、筛选前后数量及区间差异，不另生成长报告。算法入口tools/compare_block_sigma.py。

## C冻结20min：弱证据边界对照（本地）

完整traffic数据。5sigma触发、C的累计量处理及零方差有效幅度、源内gap5和全局gap3、评分排序及316个入选窗口均保持不变。原C的316个核心起止区间逐一复现一致；新版本仅修正已有窗口边界，不新增、合并或重排窗口。完成本地对照后，3sigma和2.5sigma边界版均经用户单独批准提交。

对每个窗口中实际出现的强异常，沿同一(node,metric)及同一冻结均值方差寻找同方向弱证据。连续2个观测恢复正常或相邻采样间隔>2min停止；单个正常样本可桥接，最终边界落在证据时间而非恢复等待结束时间。零方差仍用C最小有效幅度，不随3/2.5sigma放宽。
相邻原核心之间以空闲区间中点限制延伸，防止互相覆盖；总长不超过30min，超限按左右延伸长度比例分配空间，并收缩到实际证据时间，不删除或截短原核心。没有把“一般20min以上间隔”设成绝对结束后20min间隔门槛。

| 版本 | 窗口 | 改动窗口 | 平均长度/min | 中位数/min | <=3min | >=25min | 达30min |
|---|---:|---:|---:|---:|---:|---:|---:|
| 原C | 316 | 0 | 7.354 | 3 | 166 | 26 | 0 |
| 3sigma边界 | 316 | 190 | 9.905 | 7 | 114 | 36 | 12 |
| 2.5sigma边界 | 316 | 205 | 10.244 | 7 | 107 | 39 | 13 |

3sigma平均向前延伸1.047min、向后1.503min；2.5sigma分别为1.089、1.801min。延伸超过10min的窗口分别为17、19，30min限制介入均为11次。316个窗口均有内部强证据；两版均保留原核心、无重叠，JSONL格式及提交区间一致性检查通过。
优先选择3sigma边界与C的AD9.5354对比：它延伸更保守，长窗及大幅延伸更少。局部统计不证明AD提升；真实短故障过度扩展可能降低Dice，旧global-gap10的近乎持平也不能作为本次边界方法有效的证据。
6项边界测试通过，包括冻结快照不改变检测结果、同方向/持续恢复、采样缺失、零方差不放宽、时长上限及相邻核心保护；原有5项连续指标测试也通过。
精简摘要：outputs/traffic_boundary_compare/summary.json。已生成两版*_windows.jsonl和*_submit.jsonl。代码入口tools/compare_traffic_boundaries.py。

### 3sigma边界版线上回执

2026-09-13，经用户批准提交一次C_boundary_sigma3_0_submit.jsonl，共316个窗口，团队2097279104209394157。提交ID1789291507307，服务器返回当天剩余0次；其他团队额度尚未查询。

| 版本 | AD | 总分 |
|---|---:|---:|
| C原始边界 | 9.535391740361836 | 13.439501329402935 |
| C + 3sigma弱证据边界 | 11.454473729842212 | 16.33803537367783 |

AD提高1.919081989480376分，约20.1%，是该次回执时该5sigma实验路线中最高的已提交AD。保持触发和入选核心不变，仅修正边界，回执支持弱证据边界有收益；缺少逐故障匹配明细，不能分解收益来自新增Dice有效匹配、端点得分或查准率各占多少。
该次回执后对照为C冻结20min、5sigma触发、3sigma边界、源内gap5min、全局gap3min。

### 2.5sigma边界版线上回执

2026-09-13，经用户单独批准，使用团队2098685882579030065提交一次C_boundary_sigma2_5_submit.jsonl，共316个窗口。提交ID1789292140761，服务器返回该团队当天剩余4次。

| 版本 | AD | 总分 |
|---|---:|---:|
| 3sigma边界 | 11.454473729842212 | 16.33803537367783 |
| 2.5sigma边界 | 11.71012791890643 | 16.627936138084515 |

2.5sigma边界AD再提高0.255654189064218分，约2.2%；相对原C的9.535391740361836提高约2.1747分。当前最佳已提交方案更新为C冻结20min、5sigma触发、2.5sigma边界、源内gap5min、全局gap3min。
两版固定输入、触发、入选核心和边界停止/保护规则，仅放宽弱证据sigma。回执支持此次放宽有小幅收益，不据此认定继续降至2sigma一定更好。后续任何提交继续单独批准。

## 筛选node资源指标族：6sigma/8sigma融合对照

2026-09-13，读取完整八区域node数据，72节点、1,450,970行。保留当前最佳traffic的316个窗口及边界不变。node仍采用冻结20min基线、源内gap5min；筛选CPU、内存、IO、磁盘空间、进程指标的异常方向、压力幅度及互补证据，而非直接将所有sigma异常合窗。
初始物理压力门槛见tools/compare_node_family_fusion.py的RULES。这些门槛是保守实验参数，并非从真值学习的最优阈值。资源压力证据间隔<=2min形成候选，要求至少两个证据点，或严重且双指标的一分钟证据；超过30min丢弃。重叠node候选融合，距离traffic不足5min不新增窗口；node新增窗口之间20min排斥是实验性先验，不把官方“一般”误称绝对约束。本轮未引入新的拓扑加权，traffic原有规则不变。

| node阈值 | 原始指标小段 | 压力候选段 | 合并候选 | 新增窗口 | 总窗口 | 新增平均长度/min |
|---|---:|---:|---:|---:|---:|---:|
| 6sigma | 56,933 | 31 | 26 | 17 | 333 | 14.706 |
| 8sigma | 35,539 | 31 | 26 | 17 | 333 | 14.882 |

两版333个区间中331个完全一致，新增均主要为CPU/IO（15窗含CPU、6窗含IO，类别可重叠）。未选出内存/空间/进程候选，不代表没有这些故障：绝对门槛、长段丢弃及可观测性都可能漏检。样本分布中进程数最大约387、文件系统使用率最高约0.892，说明当前400进程/0.90空间等门槛可能过严；这两个最大值仅为每10行抽样统计，不能当全量最大值。
6/8sigma高置信候选数量几乎一致，不能宣称8sigma显著改善准确率。选择8sigma作一次线上补充对照；本地检查316个traffic区间全部保留、无重叠，提交格式及区间一致性通过。新增资源窗口未额外使用2.5sigma弱边界；该设置只沿用在traffic中。

### 线上回执与下一步

经用户批准使用团队2098685882579030065提交8sigma融合版，提交ID1789298570673，剩余3次。

| 版本 | AD |
|---|---:|
| traffic 5sigma触发 + 2.5sigma边界 | 11.71012791890643 |
| 保留traffic + 筛选node 8sigma补窗 | 13.500592373195124 |

AD提高1.790464454288694分，约15.3%。支持筛选资源证据后补充窗口这一方向，不证明所有新增窗口正确，缺少逐故障匹配明细，不能分解Dice/端点/查准率贡献。6sigma未提交，因仅两个区间不同，暂不优先消耗额度。
下一步优先单独调整目前未覆盖的内存/空间/进程指标族：检查全量分布与持续偏移，保留sigma和相对变化条件，逐族放宽不合理的绝对门槛，再本地比较并获批准提交。暂不同时修改traffic边界和global_gap，也不以凑齐292窗为目标。
入口tools/compare_node_family_fusion.py；3项筛选/方向/融合测试通过。精简摘要outputs/node_family_compare/summary.json，可提交文件outputs/node_family_compare/traffic25_node_family_sigma8_submit.jsonl。

## 内存/空间/进程：相对压力门槛对照（本地，未提交）

2026-09-13，固定AD13.500592373195124对应的333个窗口，不改变任何原有区间。完整72节点、1,450,970行；六个目标字段各有1,450,959个有效值。全量极值确认：内存可用率最低0.65510、swap最高0.009673、文件系统使用率最高0.89204、inode使用率最高0.02619、FD比例最高0.006974、进程数最高387。旧内存<=0.20、空间>=0.90、进程>=400等硬门槛不能覆盖这些观测。没有真值，不能把全量极值本身称为故障证据。

仍用冻结20min、8sigma、源内gap5min、压力证据gap2min、1～30min限制。新筛选要求方向和相对变化，且至少两个压力证据点，不接受孤立单点。保守档：内存可用<=0.80且下降>=0.15；空间使用>=0.80且上升>=0.08；进程>=240、增加>=80、达到基线1.8倍。较宽松档相应为内存<=0.85/下降0.08、空间>=0.75/上升0.05、进程>=200/增加50/1.5倍。变化量为比例绝对差，0.08指8个百分点。CPU/IO不重新选窗。
保留原有源内>30min长段丢弃机制；本轮两档的原始指标段均35,539、长段丢弃均751，该计数覆盖全部11个node指标，不能归因于内存等某一族。swap/FD沿用旧零方差最小幅度，可能过严，本轮不同时改动。

| 档位/指标族 | 压力候选段 | 新增窗口 | 总窗口 | 新增平均长度/min | 新增<=3min |
|---|---:|---:|---:|---:|---:|
| 保守/内存 | 2 | 1 | 334 | 12.0 | 0 |
| 保守/空间 | 7 | 4 | 337 | 14.5 | 0 |
| 保守/进程 | 2 | 0 | 333 | — | 0 |
| 保守/合并 | 11 | 5 | 338 | 14.0 | 0 |
| 较宽松/内存 | 7 | 4 | 337 | 16.25 | 1 |
| 较宽松/空间 | 10 | 5 | 338 | 14.4 | 0 |
| 较宽松/进程 | 4 | 0 | 333 | — | 0 |
| 较宽松/合并 | 21 | 9 | 342 | 15.22 | 1 |

进程候选全部与已有窗口重叠或相隔不足5min，被保护规则阻止新增；不是没有检测到进程变化。本轮固定全部333个原始区间、无重叠、所有非空新增版的提交格式及提交区间校验通过。7项node测试和11项traffic测试通过。
优先建议提交较宽松磁盘空间单类版：338窗，仅新增5窗，长度依次14/30/14/8/6min，来自5个城市的service-vm；完全保留保守档4个新增区间，仅多1个14min区间。比一次混合所有新指标更易归因；没有本轮回执，不能宣称该版一定增分。次优先较宽松内存单类版337窗；进程版区间与现有最佳相同，不值得消耗额度。合并342窗保留为后续候选，不优先提交。
入口tools/compare_node_relative_pressure.py，精简摘要outputs/node_relative_compare/summary.json；推荐可提交文件outputs/node_relative_compare/node_relative_balanced_disk_space_submit.jsonl。所有本轮新版本均未线上提交，AD13.5006仍为已验证最佳。

### 较宽松磁盘空间版线上回执

上述本地结论之后，经用户批准于2026-09-13提交一次node_relative_balanced_disk_space_submit.jsonl，338窗，团队2098685882579030065；提交ID1789305063729，当天剩余2次。

| 版本 | 窗口数 | AD |
|---|---:|---:|
| traffic + 筛选node CPU/IO 8sigma | 333 | 13.500592373195124 |
| 保留上述区间 + 磁盘空间相对压力补充 | 338 | 14.088940044851544 |

AD提升0.588347671656420分，约4.36%。当前最佳更新为338窗版本；此次只新增5个磁盘空间区间，回执支持该补充规则的整体收益，不证明5窗全部正确，不能分解逐窗Dice、端点或查准率贡献。
下一步建议以338窗固定对照，单独补充较宽松内存候选；现有337窗内存文件基于旧333窗，不能直接用于测试“在最佳版上补内存”，应先重新进行保护和融合校验。任何后续提交仍需用户批准。

## 固定338窗：内存两档对照（2026-09-14，本地未提交）

以已提交AD14.088940044851544的磁盘空间版为固定对照，重新读取完整72节点、1,450,970行node数据，使用原冻结20min、8sigma及原两档内存相对变化门槛。所有338个已有窗口均参与重叠/5min近邻保护，原区间及边界不变，不重新挑选CPU/IO/空间窗口。

| 内存档位 | 候选段 | 保护规则排除 | 新增窗 | 总窗 | 新增长度/min |
|---|---:|---:|---:|---:|---|
| 保守：可用<=80%、下降>=15个百分点 | 2 | 1 | 1 | 339 | 12 |
| 较宽松：可用<=85%、下降>=8个百分点 | 7 | 3 | 4 | 342 | 2、29、22、12 |

两档新增区间完全相同的有1个，较宽松档额外3个；新磁盘空间窗口未导致额外排除。新增均为service-vm：保守档西安1窗；较宽松档另有北大1窗、武汉2窗。仅西安12min窗为两档共有。缺少真值，不能把这些压力候选直接判为正确故障，也不能从标准化幅度很大推断置信度必然很高。
推荐优先较宽松342窗版，原因是能测试更多内存补充证据，但有2min短窗以及29min长窗的区间匹配风险。保守339窗作为后续门槛对照。校验确认两版提交文件保留全部338个已提交区间、无重叠、格式和转换前后区间一致；8项node测试通过，新增测试覆盖新磁盘窗口阻止附近内存重复开窗。
复核发现较宽松342窗的AD区间与昨天未提交的node_relative_balanced_all_submit.jsonl完全一致；并非又新增了另一组342窗，不应重复提交两者。本次明确以338窗为基准，因此下一次回执可归因于内存补充。
脚本tools/compare_node_relative_pressure.py支持--reference、--reference-ad、--families及--label-prefix。摘要outputs/node_memory_compare/summary.json；候选和可提交文件为best338_memory_conservative_memory_*及best338_memory_balanced_memory_*，位于outputs/node_memory_compare。未线上提交，本轮不消耗额度；今天额度未查询，不沿用昨天剩余2次的数字。

### 较宽松内存补充版线上回执

上述本地对照之后，经用户批准于2026-09-14提交一次best338_memory_balanced_memory_submit.jsonl，342窗，团队2098685882579030065。提交ID1789365624103，服务器确认该团队当天剩余4次评测。

| 版本 | 窗口数 | AD |
|---|---:|---:|
| traffic + node CPU/IO + 磁盘空间 | 338 | 14.088940044851544 |
| 保留上述区间 + 较宽松内存补充 | 342 | 14.55902988063768 |

AD增加0.470089835786136分，约3.34%。当前已验证最佳更新为342窗版。固定338个原区间、只新增4个内存窗口，回执支持本次内存补充的整体收益，不证明所有新增窗口正确，无法分解逐窗匹配、端点或查准率贡献。保守339窗未提交，较宽松合并旧文件与本次AD区间一致，不重复消耗额度。
下一步优先固定342个入选窗口，单独对照node资源窗口原边界与3sigma同方向弱证据边界，沿用冻结基线及30min/相邻窗口保护，不同时修改触发或global_gap。先本地生成并分析变化，再获批准提交。

## 资源窗口3sigma弱边界（2026-09-14，本地未提交）

以AD14.55902988063768对应342窗为固定对照，重新读取完整72节点、1,450,970行node数据。还原CPU/IO、磁盘空间、内存的分阶段筛选与保护，精确复现全部26个资源原区间。重新获取完整压力证据及8sigma检测时的冻结均值/标准差，不使用输出文件截断的30个高分点代替完整证据。
只沿各指标族主指标（cpu_usage、disk_io_util、memory_available_ratio、filesystem_used_ratio/inode_used_ratio）追踪同方向3sigma弱证据，不让load/读写速率等辅助指标单独拉长边界。沿用2个连续正常观测停止、相邻样本间隔>2min停止、原核心保持不变、总长<=30min及相邻原窗口空闲区间中点保护。零方差沿用原最小有效变化幅度，不因弱sigma而放宽。316个traffic记录完整保持不变。

| 项目 | 原边界 | 资源3sigma弱边界 |
|---|---:|---:|
| 总窗口数 | 342 | 342 |
| 资源窗口数 | 26 | 26 |
| 改动资源窗口 | 0 | 15 |
| 资源平均长度/min | 15.0 | 16.077 |
| 资源<=3min窗口 | 4 | 2 |
| 资源>=25min窗口 | 7 | 7 |
| 全部<=3min窗口 | 111 | 109 |

15个变化窗均来自原CPU/IO补充，9个磁盘空间/内存窗口均未变化，另2个CPU/IO窗口未变化。总延伸28min（向前8、向后20），单窗总延伸1～4min，1次30min上限介入；无重叠，不新增/删除/重排窗口。该改动局限在少量资源窗，预期不应作为大幅增分的保证。适合单变量线上对照，实际收益须回执判断，当前已验证最佳仍AD14.5590。
11项node及11项traffic测试通过；新增测试覆盖冻结基线方向/恢复、零方差不放宽及traffic记录不可改动。生成入口tools/compare_node_boundaries.py；候选及可提交文件outputs/node_boundary_compare/best342_node_boundary_sigma3_*，摘要outputs/node_boundary_compare/summary.json。本轮未提交，不消耗比赛额度。下一步在边界回执后，单独比较冻结20min均值/标准差与中位数/缩放MAD，不同时改历史窗口管理与global_gap。

### 资源3sigma弱边界线上回执：小幅退步

上述本地对照之后，经用户批准于2026-09-14提交一次best342_node_boundary_sigma3_submit.jsonl，342窗，团队2098685882579030065。提交ID1789370080591，服务器确认该团队当天剩余3次。

| 版本 | AD |
|---|---:|
| 原资源边界，当前最佳 | 14.55902988063768 |
| 资源3sigma弱边界 | 14.495349675558774 |

AD下降0.063680205078906分，约0.44%。本次只改变15个CPU/IO窗口边界，回执说明这些延伸的整体收益为负，不说明每个改动窗口都退步，也不能推断此前traffic弱边界方法无效。没有逐故障明细，无法确认损失来自过度延伸、弱证据噪声、端点或区间匹配中的哪一项。
不采用本次资源弱边界，当前最佳仍为原342窗、AD14.55902988063768，文件outputs/node_memory_compare/best338_memory_balanced_memory_submit.jsonl。不删除对照代码或输出，保留用于复盘；不继续盲目放宽资源边界sigma。下一步优先冻结20min均值/标准差与中位数/缩放MAD的基线统计对照，分别分析traffic/node，保留原基线时长、冻结机制、gap及筛选规则；先本地运行，再批准提交。
