# AD优化记忆与待验证问题

更新日期：2026-09-15。只优化连续数据AD；不修改RCA及ad_detection算法。新线上提交需批准，不自动切换团队。

## 固定对照

- 已验证最佳：345窗，AD14.894193170538037，提交1789391616452。
- traffic：历史20min均值/总体标准差冻结，强异常5sigma，指标gap5min、全局gap3min，2.5sigma弱证据边界，计数器先转速率；旧释放逻辑。
- node：旧释放8sigma、全尺度每指标幅度保护、CPU/IO19+空间5+内存5个分阶段独立补窗。traffic316窗原样保留。
- 最佳可提交文件：outputs/frozen20_guard_compare/traffic_mean_std_node_mean_std_guard_submit.jsonl。
- 当前团队2098685882579030065在2026-09-14最后回执确认剩余0次；另两队当日余额未知，次日不可沿用此数。
- 2026-09-15已跨日，三队今日余额均未核查，不能沿用昨日0次，也不据此自动提交。

## 暂放一边：traffic幅度保护

状态：待验证，暂不修改。正标准差很小时，sigma*std可能低于正常业务抖动；筛除告警历史后尺度可能进一步收缩。可比较max(sigma*std,每指标最小变化幅度)，但轻微真实故障也可能被压掉。traffic目前只在近零尺度时回退到幅度下限。node幅度保护得到正向回执，不证明traffic同样有效。
诊断应统计受下限影响的实际点、指标及最终窗口，不能凭窗口数判误报。此项不是最优先线上测试项。

## 当前优先：事件组织与长段复检

1. 相近全局候选目前可能因起点相距<=20min竞争掉一个，而非判断是否同一故障后合并。先做同指标/同方向关联、间隙正常恢复否决、总长<=30min的保守合并；保持其他阈值不变。
2. >30min指标段现在整段丢弃，可能包含真实异常核心，也可能纯噪声/正常工况迁移。保留问题段，原冻结基线下提高核心严格度，按真实异常点、方向及正常谷底拆段；无可靠短核心允许零输出，仍长的核心不强切。
3. 当前全局桶把指标小段首尾所有分钟均算证据，包括正常空白；本轮合并/复检决策使用实际观测，但不同时替换旧桶计分。后续独立测试真实点与填补点区别计分。
4. 旧全局分数按分钟累加，偏爱长窗口；目前至少3个traffic小段覆盖即可活跃，不是3台设备。后续需区分重复小段、不同指标及节点支持。
5. node靠traffic外独立补窗，近邻node候选可能被直接排除，并未统一融合其边界证据；本轮固定node规则，不同时修改。

## 基线实验结论与风险

- 旧冻结释放只在当前点正常且距上次异常>gap时释放；异常当前点跨gap只重开小段，不换旧快照，可能长期陈旧。
- 仅traffic前置释放+最近20min原始历史：343窗，AD14.056771037182006，低于最佳；实际冻结基线3.64%含此前告警。
- 前置释放+排除告警历史：444窗，AD14.25182764099717，较原始重算提高0.1951但低于最佳。实际已告警点混入为0，但暂正常覆盖不足20min达61.74%。不能称新增99窗均误报。
- 再用限龄旧正常基线补足：本地513窗，traffic触及500窗上限，中位数20min；不推荐提交。旧样本限龄60min仍不能排除正常工况迁移。
- MAD与可信缓存traffic实验重写较多区间，MAD多档触及上限；可信缓存仅改node最终区间相同。未采用为默认。
- “暂正常”是算法未告警，不是无故障真值；冷启动/热身未判定数据不能自动算正常。20样本不等于20min覆盖，补20个历史分钟也不等于连续最近20min无故障。

## 数据与规则解释

- Q5为1~30min，Q7为同一时刻一个故障；Q6间隔一般>20min是软先验。异常点间隔不是故障间隔；同一故障可能间歇告警。
- 约292个故障和平均约69min间隔不能推导每个69min块恰好一个事件。不能凑292窗或强制每块开窗。
- 起点相距20min不等于两窗口结束/开始间隔20min。不重叠及长度合法不能保证Dice匹配。
- 拓扑现实现主要设备角色组合加分，不是完整城市/节点路径验证；不能宣称已严格按真实传播路径融合。
- traffic旧(node,metric)身份与重复时间戳仍可能混合不同流/目标。需要单独核查，不在事件组织对照中顺带改数据身份。
- 低AD分数不能仅由汇总回执断言Dice不足、误报或边界误差各占多少；须真值/官方详细匹配结果或单因素对照。

## 实验纪律

保留最佳默认，新增规则可选开关；候选及诊断本地生成，精简摘要，不上传数据。一次线上仅提交一个获批变体。当前先做合并独立档、长段复检独立档及组合档，组合不优先提交；值得保留的代码/测试/复盘推送到授权GitHub仓库。

## 本轮事件组织实验（2026-09-14~15，本地，未提交）

- 5min关联合并：440个原始候选合并26对后414个，最终345窗，323原区间完全一致。但340原区间完整被包含、4个部分重叠、1个无重叠，原因是后续原NMS/弱边界仍有副作用。不能只看窗口数相同就叫无回归。
- 8/10min合并最终346/345窗，多改5~6个区间，原窗口无重叠仍1个，没有真值不能认定更好。这里调整的是候选间关联合并gap，不改指标EVENT_GAP5min或旧全局活跃gap3min。
- 补充5min参考保护档：347窗，328区间完全一致、17个原区间被扩大覆盖，原345全部被包含；19个新增/变化区间。2个过长冲突组件退回旧窗口，1个组件扩展修复、1个丢失参考恢复。1~30min、不重叠通过。参考是最佳预测，不是标注；保护也可能保留旧误报，软间隔先验不强制。
- 原版丢弃643个长指标段，其中321829个原始强异常点。首轮仅提高sigma/按方向拆核心，6/7sigma恢复77479/77305段，最终512窗且traffic触及上限、全部traffic>=25min；不可采用。
- 第二轮加入同分钟去重/双向排除、核心两侧5min内两分钟连续低于核心阈值的观测谷底（父段边缘例外）、弱边界不能穿过这些谷底。6/7sigma恢复3000/4008段，最终486/503窗，源未触上限但重写357/408区间；组合档475窗。仍不推荐直接重新联合排序。抬高sigma会改变谷底和拆段，窗口数量不单调。
- 核心谷底是低于6/7sigma的相对谷底，不是确认故障恢复；缺采不能充当谷底。仍有895/894个超过30min的核心簇未解决，不能叫895个故障，允许继续待复盘。
- 当前优先候选是5min保护档；未经线上评测，最佳仍原345/AD14.8942。后续长段宜先保留固定最佳窗口做独立补充，避免恢复噪声后挤掉原窗口，再独立研究全局真实点计分。幅度保护继续搁置。
- 入口tools/compare_event_review.py；首轮摘要outputs/event_review_compare，当前算法摘要/候选outputs/event_review_refined_compare。71项测试通过，未准备新提交格式文件、未联网提交。
